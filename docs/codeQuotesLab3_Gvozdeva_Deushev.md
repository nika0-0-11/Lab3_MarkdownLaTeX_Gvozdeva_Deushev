<!-- **Блоки кода**

Встроенный (inline) код:
Команда `git status` показывает состояние репозитория.

Многострочный код:
```python
print("Hello, Markdown!")
```
Можно указать язык: python, js, bash, csharp и др.

**Цитаты**

Цитата создаётся символом >:
> Это цитата в Markdown.
Вложенная цитата:
> Первый уровень
>> Второй уровень -->

**1. Пример команды Git в виде inline-кода:**

Для создание и мгновенного переключения ветки:
    `git switch -c feature`

**2. Блок кода на языке C++:**

    ```cpp
    #include <iostream>

    int main() {
        std::cout << "Hello friend" << std::endl;
        return 0;
    }
    ```

**3. Блок кода без указания языка:**

    ```
    print("Hello friend")
    ```

**4. Цитаты:**

> test
>> test